Before tackling these tasks, group them by what would make sense to do in the same session. Then do one of the tasks groups. When a task is done, mark as done in the list.

- [x] Remove the card-meta and tag-list from the footer on cards as they clutter the UI. Keep the backend part, so the filters keep working.
- [x] If a user has cards in the "Estudiar" section, on load default there instead of "Descubrir"
- [x] Change the top left header and page title to "Yuwenke"
- [x] Remove the "Cada ficha aparece una vez en cada sentido." part and instead have a button there for "Como funciona?". On click it opens a in-app window that explains how the app works, on a user-level.
- [x] Create and implement a cool & iconic logo. Also add favicons in all the currently relevant formats & sizes.
- [x] Implement a favorite cards feature and a favorite cards queue with that filter applied (also record favorite cards to firebase)
- [x] Sometimes I cannot see more cards in discover eventhough there should be more than 200 still available. Why is that? Could it be a bug? Lets fix that.
- [x] Make sure status and progress of cards in estudiar, descubrir, dominadas and favoritas is also stored in Firebase.

Flashcard Content:
- [ ] Add this on a new content guidelines sections on AGENTS.md implement it: The conceptos cards should only be in spanish to spanish and be made into questions to memorize the rule itself, not the translation of the rule. A correct example is: Cuantas marcas tonales puede haber por silaba? - Solo puede haber una.
- [ ] Remove mentions of the following words that we haven't learned yet (adjust the flashcards so they still make sense afterwards):
	- qingwen
	- hěn gāoxìng rènshi dàjiā.
- [x] Nombres propios deberian ser lilas (en todas las lenguas). Explicar esto en la nueva seccion de "Como funciona?""
